import React from 'react';
import { useFormik, Form, useField, useFormikContext, Formik } from 'formik';
import { MyTextInput } from './SignupForm';

function LoginForm() {
	return <h1>Login Form</h1>;
}
export default LoginForm;
